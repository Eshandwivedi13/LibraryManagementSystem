package Enums;
//enum - special datatype is case sensitive, fixed number of predefined customizable values hi ho sakti
public enum CardStatus {//we used enum so that users cannot add any random value...(they have to choose one predefined value from enum only)
    ACTIVE,
    NEW,
    EXPIRED,
    DEACTIVATED,
    BLOCKED,

}
